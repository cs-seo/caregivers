export function comingUpNextNotice() {
  return "Add household defaults once, or subscribe these sits so Google and Apple Calendar stay in sync.";
}

export function comingUpNextLinks() {
  return [
    { href: "/dashboard/household", label: "Open household defaults" },
    { href: "/dashboard/calendar", label: "Subscribe your calendar" },
  ];
}
